问题分析
1. 已解决
   - 本案例中使用简单工厂解决了：只针对 CPU 和主板的接口编程，不需要针对具体的 CPU 和主板。
2. 没有解决的问题
   - CPU 对象和主板对象其实是有关系的，是需要相互匹配的。在上面的实现中，并没有维护这种关联关系，CPU 和主板是由客户随意选择的。
   - 如在调用 makeComputer 时，传入参数为(1,2)，控制台会输出如下结果：
     now in Intel CPU,pins=1156
     now in MSIMainboard,cpuHoles=939
   - 造成针脚和插孔不匹配的情况。
