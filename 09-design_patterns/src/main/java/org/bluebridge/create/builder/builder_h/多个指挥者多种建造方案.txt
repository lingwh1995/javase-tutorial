多个指挥者多种建造方案
1. 概述
   - 一个指挥者对应一种建造方案，多个指挥者对应多种建造方案。
2. 优点
   - 可以让你改变产品的内部表示。
   - 封装构建和代表性的代码。
   - 提供对构建过程步骤的控制。
3. 缺点
   - 需要为各种不同类型的产品单独创建 ConcreteBuilder。
4. 使用场景
   - 当创建复杂对象的算法应该独立于该对象的组成部分以及它们的装配方式时。
   - 当构造过程必须允许被构造的对象有不同表示时(相同的方法，不同的执行顺序，产生不同的结果时)。
