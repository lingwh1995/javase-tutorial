类适配器
1. 概述
   - Adapter 类继承 src 类，实现 dst 接口，完成 src -> dst 的适配。
   - 220V 电压 -> 变压器(适配器) -> 5V 电压。
2. 类适配器模式分析
   - Java 是单继承机制，类适配器需要继承 src，会导致 dst 必须是一个接口。
   - src 类的方法在 Adapter 中都会暴露出来，也增加了使用成本。
   - 由于适配器继承了 src 类，于是可以重写 src 类中的方法，使得 Adapter 的灵活性增强了。