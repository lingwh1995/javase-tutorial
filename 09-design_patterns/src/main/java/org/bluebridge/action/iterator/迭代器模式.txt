迭代器模式:遍历元素时屏蔽目标元素的差异,调用同一个方法既可以遍历集合,也可以遍历数组其关键思想在于把聚
        合对象的遍历和访问从聚合对象中分离出来,放入到单独的迭代器中,这样聚合对象会变得简单一些,而且
        迭代器和聚合对象可以独立的变化和发展,会大大的增强系统的灵活性.
深入理解:
    1.访问聚合对象内容的时候，无需暴露聚合对象的内部表示,从而提高聚合对象的封装性
    2.将聚合对象的内容和具体的迭代算法分离
    3.在进行迭代的时候,可以设置迭代策略,完成"控制访问"
迭代器模式本质:
    控制访问聚合对象中的元素