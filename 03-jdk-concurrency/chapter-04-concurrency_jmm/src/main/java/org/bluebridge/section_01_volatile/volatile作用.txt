volatile 作用
1. 保证多线程间可见性
   - volatile 修饰的变量，在写操作完成后会立即刷新到主内存，其他线程读取时能读到最新值，避免读到脏数据。
2. 防止指令重排序
   - volatile 通过内存屏障禁止指令重排序，保证代码的执行顺序符合预期。
3. 不保证原子性
   - volatile 不能保证复合操作的原子性，例如 i++ 这类操作仍需通过 synchronized 或 Atomic 类来保证。
