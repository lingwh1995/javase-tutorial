双重检查单例模式(Double-Checked Locking)
1. 概念
   - 双重检查锁是单例模式的一种实现，在懒加载的基础上通过 synchronized 保证线程安全，同时减少不必要的锁竞争开销。
2. 关键点
   - 第一次检查：实例已存在则直接返回，避免进入同步块。
   - 同步块：保证只有一个线程执行实例创建。
   - 第二次检查：进入同步块后再次判断，防止多个线程同时通过第一次检查后重复创建实例。
   - volatile：实例变量需要用 volatile 修饰，防止指令重排序导致返回未完全初始化的实例。