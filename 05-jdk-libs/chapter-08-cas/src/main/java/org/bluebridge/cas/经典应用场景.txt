经典应用场景(Classic Application Scenarios)
1. 判断 List 中元素是否重复
   - 借助 Set 的去重特性快速判断集合中是否存在重复元素。
2. 线程睡眠
   - 线程休眠指定时间，常用于模拟耗时操作或控制执行节奏。
3. 统计某段代码执行时间
   - 记录代码执行前后的时间戳，计算耗时。
4. 模板字符串
   - 动态拼接字符串，替代传统字符串连接。
5. 数据类型转换(数据类型映射)
   - 在不同数据类型之间进行转换。
6. 多条件判断简化写法
   - 用更简洁的写法替代冗长的 if-else 多条件判断。
