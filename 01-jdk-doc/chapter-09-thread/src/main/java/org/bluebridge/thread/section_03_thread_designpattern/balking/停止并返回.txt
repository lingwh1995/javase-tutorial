Balking 模式(停止并返回)
1. 基本概念
   - 多线程模式下使用 Observer 模式时，有时会用到 Balking 模式。
   - Balking 模式的特点就是不进行等待：当守护条件不成立时，想要立即返回并进入下一个操作，就可以使用 Balking 模式，这有利于提高程序响应速度。
   - 而 Guarded Suspension 模式则相反：当守护条件不成立时，线程会一直等待，直到守护条件成立后才继续执行。
2. 状态判断
   - 对象本身拥有状态，该对象只有在自身状态合适时才会进行处理，否则不会进行处理(balk 直接返回)。