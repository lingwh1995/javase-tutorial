序列化与 static 变量
1. 静态变量属于类级别，不是对象状态的一部分，因此不参与序列化。
2. 静态变量的值由类加载器在类加载时初始化，与对象实例无关，反序列化时也不会恢复其值。
3. 将静态变量声明为 transient 是没有用处的: 静态变量本身就不参与序列化，声明 transient 纯属多余。
4. 注意: transient 只对实例变量生效，用于声明"该实例字段不参与序列化"。